# celsius-to-fahrenheit-in-c
This repository contains a C program that converts temperature from Celsius to  Fahrenheit using the formula F = (9 × C / 5) + 32. The program demonstrates the  use of functions and floating-point arithmetic in C.
